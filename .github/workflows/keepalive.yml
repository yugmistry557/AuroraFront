name: Keep HF Space Awake

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping HuggingFace Space
        run: |
          curl -s https://iamrabbit-aurora2.hf.space/api/telemetry
